/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import * as beet from '@metaplex-foundation/beet';
import * as web3 from '@solana/web3.js';
import * as beetSolana from '@metaplex-foundation/beet-solana';
export var collectionPolicyDiscriminator = [
    203, 231, 77, 202, 39, 103, 152, 232,
];
/**
 * Holds the data for the {@link CollectionPolicy} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
var CollectionPolicy = /** @class */ (function () {
    function CollectionPolicy(rate, epoch, minimumStakeTime, interactionFrequency, attenuation, permanentPolicy, timeCapped) {
        this.rate = rate;
        this.epoch = epoch;
        this.minimumStakeTime = minimumStakeTime;
        this.interactionFrequency = interactionFrequency;
        this.attenuation = attenuation;
        this.permanentPolicy = permanentPolicy;
        this.timeCapped = timeCapped;
    }
    /**
     * Creates a {@link CollectionPolicy} instance from the provided args.
     */
    CollectionPolicy.fromArgs = function (args) {
        return new CollectionPolicy(args.rate, args.epoch, args.minimumStakeTime, args.interactionFrequency, args.attenuation, args.permanentPolicy, args.timeCapped);
    };
    /**
     * Deserializes the {@link CollectionPolicy} from the data of the provided {@link web3.AccountInfo}.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    CollectionPolicy.fromAccountInfo = function (accountInfo, offset) {
        if (offset === void 0) { offset = 0; }
        return CollectionPolicy.deserialize(accountInfo.data, offset);
    };
    /**
     * Retrieves the account info from the provided address and deserializes
     * the {@link CollectionPolicy} from its data.
     *
     * @throws Error if no account info is found at the address or if deserialization fails
     */
    CollectionPolicy.fromAccountAddress = function (connection, address, commitmentOrConfig) {
        return __awaiter(this, void 0, void 0, function () {
            var accountInfo;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, connection.getAccountInfo(address, commitmentOrConfig)];
                    case 1:
                        accountInfo = _a.sent();
                        if (accountInfo == null) {
                            throw new Error("Unable to find CollectionPolicy account at ".concat(address));
                        }
                        return [2 /*return*/, CollectionPolicy.fromAccountInfo(accountInfo, 0)[0]];
                }
            });
        });
    };
    /**
     * Provides a {@link web3.Connection.getProgramAccounts} config builder,
     * to fetch accounts matching filters that can be specified via that builder.
     *
     * @param programId - the program that owns the accounts we are filtering
     */
    CollectionPolicy.gpaBuilder = function (programId) {
        if (programId === void 0) { programId = new web3.PublicKey('2HLsq8QGhRnUUwuukCKLNdpvNc4utW6AQVV1VoY9jgEd'); }
        return beetSolana.GpaBuilder.fromStruct(programId, collectionPolicyBeet);
    };
    /**
     * Deserializes the {@link CollectionPolicy} from the provided data Buffer.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    CollectionPolicy.deserialize = function (buf, offset) {
        if (offset === void 0) { offset = 0; }
        return collectionPolicyBeet.deserialize(buf, offset);
    };
    /**
     * Serializes the {@link CollectionPolicy} into a Buffer.
     * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
     */
    CollectionPolicy.prototype.serialize = function () {
        return collectionPolicyBeet.serialize(__assign({ accountDiscriminator: collectionPolicyDiscriminator }, this));
    };
    Object.defineProperty(CollectionPolicy, "byteSize", {
        /**
         * Returns the byteSize of a {@link Buffer} holding the serialized data of
         * {@link CollectionPolicy}
         */
        get: function () {
            return collectionPolicyBeet.byteSize;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Fetches the minimum balance needed to exempt an account holding
     * {@link CollectionPolicy} data from rent
     *
     * @param connection used to retrieve the rent exemption information
     */
    CollectionPolicy.getMinimumBalanceForRentExemption = function (connection, commitment) {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                return [2 /*return*/, connection.getMinimumBalanceForRentExemption(CollectionPolicy.byteSize, commitment)];
            });
        });
    };
    /**
     * Determines if the provided {@link Buffer} has the correct byte size to
     * hold {@link CollectionPolicy} data.
     */
    CollectionPolicy.hasCorrectByteSize = function (buf, offset) {
        if (offset === void 0) { offset = 0; }
        return buf.byteLength - offset === CollectionPolicy.byteSize;
    };
    /**
     * Returns a readable version of {@link CollectionPolicy} properties
     * and can be used to convert to JSON and/or logging
     */
    CollectionPolicy.prototype.pretty = function () {
        var _this = this;
        return {
            rate: (function () {
                var x = _this.rate;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            epoch: (function () {
                var x = _this.epoch;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            minimumStakeTime: (function () {
                var x = _this.minimumStakeTime;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            interactionFrequency: (function () {
                var x = _this.interactionFrequency;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            attenuation: this.attenuation,
            permanentPolicy: this.permanentPolicy,
            timeCapped: (function () {
                var x = _this.timeCapped;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
        };
    };
    return CollectionPolicy;
}());
export { CollectionPolicy };
/**
 * @category Accounts
 * @category generated
 */
export var collectionPolicyBeet = new beet.BeetStruct([
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['rate', beet.u64],
    ['epoch', beet.i64],
    ['minimumStakeTime', beet.i64],
    ['interactionFrequency', beet.i64],
    ['attenuation', beet.u32],
    ['permanentPolicy', beet.bool],
    ['timeCapped', beet.i64],
], CollectionPolicy.fromArgs, 'CollectionPolicy');
